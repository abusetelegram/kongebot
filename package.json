{
  "scripts": {
    "build": "zip -r release.zip . -x '*.git*'  -x '*node_modules*'",
    "start:webhook": "node webhook.js"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "grapheme-splitter": "^1.0.4",
    "telegraf": "^3.25.0",
    "telegraf-ratelimit": "^2.0.0"
  }
}
